// This file is auto-generated by @codewithagents/openapi-react-query — do not edit

import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { createElement, type ReactNode } from 'react'

/**
 * Creates a QueryClient pre-configured for testing.
 *
 * Differences from the production default:
 * - `retry: false`  — tests should not retry on failure
 * - `gcTime: 0`     — no garbage-collection delay between tests
 */
export function createTestQueryClient(): QueryClient {
  return new QueryClient({
    defaultOptions: {
      queries: {
        retry: false,
        gcTime: 0,
      },
      mutations: {
        retry: false,
      },
    },
  })
}

/**
 * Returns a wrapper component that provides a `QueryClientProvider`.
 * Pass it as the `wrapper` option to `renderHook` from `@testing-library/react`.
 *
 * @example
 * ```ts
 * const queryClient = createTestQueryClient()
 * const wrapper = createWrapper(queryClient)
 * const { result } = renderHook(() => useListTasks(), { wrapper })
 * ```
 */
export function createWrapper(
  queryClient: QueryClient
): ({ children }: { children: ReactNode }) => ReactNode {
  return function Wrapper({ children }: { children: ReactNode }): ReactNode {
    return createElement(QueryClientProvider, { client: queryClient }, children)
  }
}
